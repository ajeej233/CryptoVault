jdk: openjdk11

before_install:
  - echo "SKIPPING BUILD - USING PRE-BUILT AAR FILE"
  - mkdir -p build/libs
  - find . -name "*.aar" -exec cp {} build/libs/ \;
  - echo "AAR files copied:"
  - ls -la build/libs/

install:
  - echo "Installation complete - using pre-built AAR"

build: off